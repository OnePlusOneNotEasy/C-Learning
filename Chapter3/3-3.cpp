#include<vector>
#include<iostream>
#include<string>
using std::cin;
using std::cout;
using std::string;
using std::vector;
using std::endl;

int main()
{
	// string word;
	// vector<string> text;
	// while(cin >> word)
	// 	text.push_back(word);
    
 //    vector<int> ivec;
 //    for(vector<int>::size_type ix = 0; ix != 10; ++ix)
 //    	ivec.push_back(ix);
    
    // vector<int> ivec;
    // int ival;
    // while(cin >> ival)
    // 	ivec.push_back(ival);
    // if(ivec.size() == 0){
    // 	cout << "No elements?!" << endl;
    // 	return -1;
    // }
    // cout << endl;
    // for(vector<int>::size_type ix = 0; ix < ivec.size()-1; ix = ix + 2)
    // {
    // 	cout << ivec[ix] + ivec[ix + 1] << "\t";
    // 	if((ix + 1) % 6 == 0)
    // 		cout << endl;
    // }
    // if(ivec.size() % 2 != 0)
    // 	cout << endl << ivec[ivec.size() - 1] << endl;
    // else
    // 	cout << endl;

    // vector<int>ivec;
    // int ival;
    // while(cin >> ival)
    // 	ivec.push_back(ival);
    // if(ivec.size() == 0)
    // {
    // 	cout << "No elements?!" << endl;
    // 	return -1;
    // }
    // cout << endl;
    // vector<int>::size_type cnt = 0;
    // for(vector<int>::size_type first = 0, last = ivec.size() - 1; first < last; ++first, --last)
    // {
    // 	cout << ivec[first] + ivec[last] << "\t";
    // 	++cnt;
    // 	if(cnt % 6 == 0)
    // 		cout << endl;
    // }
    // if(ivec.size() % 2 != 0)
    // 	cout << endl << "The center element is not been summed and its value is " << ivec[ivec.size() / 2] << endl;
    // else
    // 	cout << endl;






	return 0;
}