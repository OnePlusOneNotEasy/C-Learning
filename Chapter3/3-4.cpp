#include<vector>
#include<iostream>
#include<string>
using std::cout;
using std::cin;
using std::vector;
using std::string;
using std::endl;

int main()
{
	// vector<int> ivec;
	// int i;
	// while(cin >> i)
	// 	ivec.push_back(i);
	// cout << endl;
	// for(vector<int>::size_type idx = 0; idx != ivec.size(); ++ idx)
	// 	cout << ivec[idx] << ' ';
	// cout << endl;
    
 //    for(vector<int>::iterator iter = ivec.begin(); iter != ivec.end(); ++iter)
 //    	*iter = 0;

	// vector<char> text;
	// char c;
	// while(cin >> c)
	// 	text.push_back(c);
	// for(vector<char>::const_iterator iter = text.begin(); iter != text.end(); ++iter)
	// 	*iter = ' ';//cannot assign to return value because function '*operator' returns a const value
	
    // vector<int> nums(10);
    // const vector<int>::iterator cit = nums.begin();
    // *cit = 1;//cit can change its underlying element
    // ++cit;//cannot change the value of cit

    // const vector<int> nines(10, 9);
    // vector<int>::const_iterator cit = nines.begin();
    // ++cit;
	




	return 0;


}